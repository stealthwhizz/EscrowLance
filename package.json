{
  "name": "chainescrow",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend && npm install --prefix blockchain",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:blockchain": "npm run node --prefix blockchain",
    "test": "npm test --prefix backend && npm run test --prefix blockchain"
  }
}
